// import TDesign from 'tdesign-vue-next'
// import { TDesignResolver } from 'unplugin-vue-components/resolvers'
// 引入组件库的少量全局样式变量
import 'tdesign-vue-next/es/style/index.css'

import { App } from 'vue'

/**
 * 全量注册TDesign所有组件
 * @param app Vue实例
 */
export default function registerTDesign(app: App) {
  // 已使用自动导入插件
  // app.use(TDesign);
}
